import { Metadata } from "next";

// META TAGS
export const metadata: Metadata = {
  title: "Redefinir senha | Insira sua nova senha",
};

// PÁGINA
export default function RedefinirSenhaPage() {
  return (
    <section className="redefinir-senha-page-wrapper">
      <p>Redefinir senha</p>
    </section>
  );
}
